{
  "name": "app.koda.monitor",
  "version": "1.0.0",
  "description": "Koda Server Monitor",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/preferences": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
